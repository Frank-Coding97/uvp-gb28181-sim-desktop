{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面端默认权限:主窗口 + 核心 IPC。",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
